export const metadata = {
  title: 'Categoría | Flowix',
  description: 'Productos por categoría',
};

export default function CategoriaLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
