export const metadata = {
  title: 'Categorías | Flowix',
  description: 'Explora nuestras categorías de productos',
};

export default function CategoriasLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
